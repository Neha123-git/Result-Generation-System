# Report Card System Frontend

## Run
- `npm install`
- `npm run dev`

## Notes
- Uses Basic Auth credentials for API calls.
- Default credentials: `teacher1 / teacher123`.
- Backend should run at `http://localhost:8080`.
