version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: report_card_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
